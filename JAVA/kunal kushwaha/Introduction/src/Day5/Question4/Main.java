package Day5.Question4;

public class Main {
    /*
     * 4. Write a Java program to create a Animal interface with a method called
     * bark() that takes no arguments and returns void. Create a Dog class that
     * implements Animal and overrides speak() to print "Dog is barking".
     */
    public static void main(String[] args) {
        Dog d1 = new Dog();
        d1.bark();
    }
}
